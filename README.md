# First PR practice

Add a file named `names/YOUR-GITHUB-USERNAME.txt` with a sentence about yourself (or anything you like). Make a branch and open a pull request to `main`. Keep each submission in its own file.
